<<<<<<< SEARCH
    try {
        const data = await NttService.getTrackingDetails(txHash);
        if (data) {
            setTrackingData(data);
            context?.notify('success', 'Wormhole Attestation Found');
        } else {
            context?.notify('error', 'No attestation found yet. It may take a few minutes.');
        }
    } catch (e) {
        context?.notify('error', 'Failed to reach Wormhole Explorer.');
    } finally {
        setIsTracking(false);
    }
  };
=======
    try {
        const results = await NttService.getTrackingDetails([txHash]);
        if (results && results[0]) {
            setTrackingData(results[0]);
            context?.notify('success', 'Wormhole Attestation Found');
        } else {
            context?.notify('error', 'No attestation found yet. It may take a few minutes.');
        }
    } catch (e) {
        context?.notify('error', 'Failed to reach Wormhole Explorer.');
    } finally {
        setIsTracking(false);
    }
  };
>>>>>>> REPLACE
