# .gitguardian.yaml
# Configure GitGuardian to ignore safe patterns and files

version: 2
secret:
  ignore-paths:
    - "tests/*.test.ts"
    - "docs/**/*.md"
    - "README.md"
    - ".gitleaksignore"
